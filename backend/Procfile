web: alembic upgrade head && exec uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers ${WEB_CONCURRENCY:-2} --proxy-headers --forwarded-allow-ips=*
worker: celery -A app.core.celery_app.celery_app worker --loglevel=info --beat --concurrency=${CELERY_CONCURRENCY:-2}
